# `@codememory/cloud-db`

Planned package for cloud-only database capabilities.

Responsibilities:

- PostgreSQL schema
- Drizzle migrations
- Cloud `DataSource` implementation
- Multi-user/team data isolation helpers

This package should not depend on Tauri, SQLite, or local desktop command execution.
